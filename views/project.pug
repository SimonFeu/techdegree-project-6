extends layout.pug

block content 
  nav.grid-container.portfolio-breadcrumb
    a(href='/') ← Back
  article.grid-container.portfolio-intro.portfolio-project
    .grid-x
      .cell.small-6.medium-9.large-9

        //- Adds the project titel out of the received project data
        h1=project.project_name

        //- Adds the project description out of the received project data
        p.lead.text-light
          | #{project.description}

      .cell.portfolio-meta.small-6.medium-2.large-3
        h6 Technologies

        //- uses the Pug each iteration to create the li elements with the used technologies
        //- Therefore the received project data is used 
        ul
          each technologie in project.technologies
            li #{technologie}

        
        a.button.expanded(href=`${project.live_link}`, target="_blank") Live Demo
        a.button.expanded(href=`${project.github_link}`, target="_blank") GitHub Repo
    

    img.image(src=`${project.image_urls[1]}`)
    img.image(src=`${project.image_urls[2]}`)
    img.image(src=`${project.image_urls[3]}`)